Following the same structure as in templates: first create a new fold inside the given App folder called 
static, and within it add a new fold with the name of the app again. 

Static folder is where all the styling files i.e. CSS are stored so they can be referenced in the HTML docs 
